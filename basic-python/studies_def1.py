#Page 37 to 40
#functions in python its an (first class objects)
#so the functions can receive parameters like variables
#parameters and more

def sum(a, b):
    return a + b
c = sum(1,3)
print(c)
#Arguments > FUNCTION > return the values
#
#INPUT     > FUNCTION > OUTPUT

#standard argument values

def salario_descontado_imposto(salario, imposto=27.):
    return salario - (salario * imposto * 0.01)

#reference pythoniluminado.netlify.app/funcoes
